# docker compose 用の環境変数サンプル
# このファイルを `.env` にコピーして値を埋めてください。
# docker-compose.yml が ${...} で参照します。

# FastAPI のセッション署名キー (任意の長いランダム文字列)
SESSION_SECRET_KEY=change-me-to-a-long-random-string

# OpenAI API キー (妖精の埋め込み生成・プラン生成に利用)
OPENAI_API_KEY=sk-...
